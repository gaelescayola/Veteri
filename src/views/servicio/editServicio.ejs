<div class="box">
    <h1 class="title">Editar Servicio</h1>
    <form action="/servicios/edit/<%= servicio.id %>" method="POST">
        <div class="field">
            <label class="label">Nombre del Servicio</label>
            <div class="control">
                <input class="input" type="text" name="name" value="<%= servicio.name %>" required>
            </div>
        </div>
        <div class="field">
            <label class="label">Descripción</label>
            <div class="control">
                <textarea class="textarea" name="description" required><%= servicio.description %></textarea>
            </div>
        </div>
        <div class="control">
            <button class="button is-primary" type="submit">Guardar Cambios</button>
        </div>
    </form>
</div>
